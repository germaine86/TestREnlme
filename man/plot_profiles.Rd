% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_profiles}
\alias{plot_profiles}
\title{Plot raw individual trajectories}
\usage{
plot_profiles(
  data,
  group,
  time,
  response,
  subjects = NULL,
  mean_profile = TRUE,
  highlight = NULL,
  title = "Individual profiles"
)
}
\arguments{
\item{data}{A \code{data.frame} containing the data.}

\item{group}{Character. Name of the grouping variable.}

\item{time}{Character. Name of the time variable.}

\item{response}{Character. Name of the response variable.}

\item{subjects}{Optional character or integer vector of subject IDs to
plot. If \code{NULL} (default) all subjects are plotted.}

\item{mean_profile}{Logical. Whether to superimpose the group-level mean
trajectory as a bold line. Default \code{TRUE}.}

\item{highlight}{Optional character or integer vector of subject IDs to
draw in a contrasting colour (e.g., subjects excluded by the
condition-number filter).}

\item{title}{Character. Plot title. Default \code{"Individual profiles"}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Produces a spaghetti plot of the raw observed trajectories for all
subjects (or a selected subset), with an optional group-level mean
profile and subject highlighting.
}
\examples{
d <- as.data.frame(Theoph)
plot_profiles(d, group = "Subject", time = "Time", response = "conc")

}
